export interface Project {
  repoUrl: string;
  title: string;
  description: string;
  stack: string;
}

export interface Post {
  title: string;
  date: string;
  subtitle: string;
  slug: string;
  tags: string;
}
